name: fastor_app_ui_widget
description: Coding Faster By Build Fast UI Widget
version: 1.0.196
homepage: https://fastor-app.com

environment:
  sdk: ">=2.17.3 <3.0.0"
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter

  #not fastor = package
  hexcolor:
  #easy_localization: cause null safty error
  universal_io:
  cached_network_image:
  shared_preferences: #^2.0.15
  flutter_spinkit: #^5.1.0
  need_resume:
  internet_connection_checker:

  #chart
  fl_chart: #^0.62.0

  #firebase
#  firebase_analytics:
#  firebase_core:

  #fastor sub library
  log_debug:

  #network
  http:
  dio:
  pretty_dio_logger: ^1.1.1
  cross_file: # ^0.3.3+4

  # plugin stop supported, so copied to fastor hard copy
  # flutter_clean_calendar: # ^1.1.0
  intl: # ^0.18.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^1.0.0

false_secrets:
  - /lib/firebase_options.dart


flutter:
